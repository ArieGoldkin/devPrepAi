# DevPrep AI - Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# === Anthropic Claude API Configuration ===
# Get your API key from: https://console.anthropic.com/
# IMPORTANT: API key should NOT have NEXT_PUBLIC_ prefix (server-side only)
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# === Optional: Override Default Model ===
# Default: claude-haiku-4-5-20251001 (Haiku 4.5 - cost-optimized)
# Cost: $1/M input tokens, $5/M output tokens, 2x faster than Sonnet
# Uncomment to override (not recommended unless you have specific needs):
# NEXT_PUBLIC_ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# NOTE: Temperature and max tokens are defined in code (lib/claude/config.ts)
# This keeps business logic in version control and allows PR reviews

# === API Configuration ===
# Base URL for your API (development vs production)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api

# API version
NEXT_PUBLIC_API_VERSION=v1

# === Cache Configuration ===
# Cache TTL in milliseconds (default: 5 minutes)
NEXT_PUBLIC_CACHE_TTL=300000

# Maximum cache size (number of entries)
NEXT_PUBLIC_CACHE_MAX_SIZE=100

# Cache storage type: memory | localStorage | sessionStorage
NEXT_PUBLIC_CACHE_STORAGE=localStorage

# === Rate Limiting ===
# Requests per minute limit
NEXT_PUBLIC_RATE_LIMIT=50

# === Development/Debug Settings ===
# Enable debug logging
NEXT_PUBLIC_DEBUG=false

# Log level: error | warn | info | debug
NEXT_PUBLIC_LOG_LEVEL=info

# === Security ===
# Enable/disable analytics
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# === Feature Flags ===
# Enable experimental features
NEXT_PUBLIC_EXPERIMENTAL_FEATURES=false

# Enable response caching
NEXT_PUBLIC_CACHE_ENABLED=true

# Enable request compression
NEXT_PUBLIC_COMPRESSION_ENABLED=true

# === Performance ===
# Request timeout in milliseconds
NEXT_PUBLIC_REQUEST_TIMEOUT=30000

# Maximum retries for failed requests
NEXT_PUBLIC_MAX_RETRIES=3

# Retry delay in milliseconds
NEXT_PUBLIC_RETRY_DELAY=1000

# === Mock Services (Development) ===
# IMPORTANT: Use these flags to control mock vs real Claude API
#
# USE_MOCK_API: Server-side mock control (API routes use this)
# - Set to 'true': Uses mock data, saves API credits, no Claude API calls
# - Set to 'false': Uses real Claude API (requires ANTHROPIC_API_KEY)
USE_MOCK_API=true

# NEXT_PUBLIC_USE_MOCK_API: Client-side mock control (not currently used)
# - Kept for future client-side mock features
NEXT_PUBLIC_USE_MOCK_API=true